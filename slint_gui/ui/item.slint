import { GroupBox } from "std-widgets.slint";
export component Item inherits GroupBox {
    vertical-stretch: 0;
    Rectangle {
        border-radius: 3px;
        border-width: 2px;
        border-color: #000;
        @children
    }
    
}
